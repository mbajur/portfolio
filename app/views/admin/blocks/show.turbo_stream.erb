<turbo-stream action="replace" target="<%= dom_id(@block) %>">
  <template>
    <%= render partial: @block.to_partial_path(:preview), formats: [:html], locals: { block: @block } %>
  </template>
</turbo-stream>
